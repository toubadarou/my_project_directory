{% extends 'base.html.twig' %}

{% block title %}Classe
{% endblock %}

{% block body %}
	<div class="container d-flex flex-column justify-content-center align-items-center border">


		<h1>Classe</h1>

		<table class="table table-striped rounded-3 table-hover">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ classe.id }}</td>
				</tr>
				<tr>
					<th>Libelle</th>
					<td>{{ classe.libelle }}</td>
				</tr>
				<tr>
					<th>Filiere</th>
					<td>{{ classe.filiere }}</td>
				</tr>
				<tr>
					<th>Niveau</th>
					<td>{{ classe.niveau }}</td>
				</tr>
			</tbody>
		</table>
	</div>
	<div class="d-flex justify-content-evenly">

		<a name="" id="" class="btn btn-secondary" href="{{ path('app_classe_index') }}" role="button">retour au liste</a>
		<a name="" id="" class="btn btn-secondary" href="{{ path('app_classe_edit', {'id': classe.id}) }}" role="button">éditer</a>
		{{ include('classe/_delete_form.html.twig') }}
	</div>
</div>{% endblock %}
