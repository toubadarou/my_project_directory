<form method="post" action="{{ path('app_a_c_delete', {'id': a_c.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ a_c.id) }}">
    <button class="btn">Delete</button>
</form>
